@model IEnumerable<CarrotCake.CMS.Plugins.PhotoGallery.tblGalleryImage>

@{
    ViewBag.Title = "View";
}

<h2>View</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GalleryImage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageOrder)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GalleryID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GalleryImage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImageOrder)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GalleryID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GalleryImageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GalleryImageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GalleryImageID })
        </td>
    </tr>
}

</table>
